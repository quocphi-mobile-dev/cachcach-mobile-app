import 'package:cachcach/core/extensions/extensions.dart';

import '../label.dart';

/// japanese definition
final jaJP = <Label, String>{
  /// A
  Label.applicationLabel: "Cach Cach",
}.map((key, value) => MapEntry(key.text, value));
